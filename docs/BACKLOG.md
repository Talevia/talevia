# Backlog

`/iterate-gap` 的驱动源。每个 cycle 的流程：

1. 读这个文件,挑**最靠上的未完成**任务（P0 → P1 → P2,同档内按出现顺序）。
2. plan → 实现 → 验证 → 归档决策 → commit + push。
3. 在 iteration 的 commit 里顺手把本任务的 bullet 从这个文件里删掉（不是打钩,直接删）,保持列表紧凑。
4. 如果这个文件的 P0/P1/P2 三档都已清空（或文件不存在）,`/iterate-gap` 会按 skill 里的 rubric 分析一次性生成 **20 条新任务**写回这个文件、commit,然后**在同一个 cycle 里继续**挑新生成列表里第 1 条开干。

任务描述只给 **Gap（现状 / 痛点）** 和 **方向（期望动的东西）** —— 细节留给 cycle 里的 plan 阶段现读代码决定。Rubric 轴用 `§5.x` 标。

---

## P0 — 高杠杆、下一步就该动

## P1 — 中优,做完 P0 再排

- **debt-split-clip-action-tool-input-seed** — Cycle 44's clip_action consolidation grew the file from 291 → 377 LOC (+86 LOC for 3 new verbs + 6 new data classes + 3 new result types). Now 12 verbs, ~16 nullable Input fields, ~13 Output result lists. Pattern matches the SessionActionTool 606-LOC trajectory (14 verbs, 17 nullable fields). **方向：** observational. **触发条件：** ClipActionTool.kt ≥ 500 LOC OR verb count > 14 → split via the same sealed-Input phasing as session_action. Rubric §5.6 / §3a-3。Milestone §later. · skipped 2026-05-02: trigger 条件未触发 — 377 < 500 LOC, 12 < 14 verbs (cycle 65 no-change since cycle 62 repopulate). Re-evaluate when ClipAction grows past threshold.
- **debt-unified-dispatcher-select-plugin-shape** — `project_query` 现已 22 selects；`rejectIncompatibleProjectQueryFilters` 已 24 规则；dispatcher 仍是单文件 monolith。**方向：** 当 select 数 ≥ 25 OR 规则 ≥ 30 时实施 select-plugin registry 或按主题拆 dispatcher。Rubric 外 / 顺手记录。Milestone §later. · skipped 2026-05-02: trigger 条件未触发 — 22 < 25 selects, 24 < 30 rules (cycle 65 no-change since cycle 62 repopulate). Re-evaluate when count grows past threshold.
- **m9-binding-conflict-detection-seed** — VISION §3.3 / M9-candidate. Multi-binding clips 当前 blind-concat-folded in `AigcPipeline.foldPrompt(...)`. **方向：** static `project_query(select=binding_conflicts)` scan over each clip's bound source nodes. **触发条件：** M9 promotion (§4 GUI mostly done) OR real user-report. Rubric §3.3 / §5.5。Milestone §later. · skipped 2026-05-02: trigger 条件未触发 — M8 still current (driver-gated per MILESTONES.md), no user-report. Re-evaluate when M9 promotes or real driver appears.

## P2 — 记债 / 观望

- **debt-tool-count-snapshot-56-2026-05-02-v6** — R.5 #1 + R.6 #1 scan refresh @ 2026-05-02 (cycle 62): `core/tool/builtin/**/*Tool.kt` count = **56** (was 55 cycle 52 baseline; **net +1** because cycle 61 a6a79b88 introduced `ProjectActionDispatcherTool` while keeping the 4 underlying tools registered per AIGC arc phase 1 precedent — phase 2 will unregister, dropping to 52 net -3 from baseline). **方向：** observational. **触发条件：** next repopulate count > 56 OR new prefix-group ≥ 3 same-prefix tools → promote `debt-shrink-tool-spec-surface` 到 P0/P1. Rubric §5.6。Milestone §later.
- **debt-todo-snapshot-0-2026-05-02-v6** — R.5 #6 anchored-regex scan @ 2026-05-02 (cycle 62): 0 code-comment-form TODO/FIXME/HACK/XXX in `core/commonMain` (unchanged since cycle 31, now 31 cycles consecutive). **方向：** observational. **触发条件：** next repopulate anchored-regex count > 0 → real `// TODO:` debt landed. Rubric §5.6。Milestone §later.
- **debt-long-file-snapshot-2026-05-02-v8** — R.5 #4 scan refresh @ 2026-05-02 (cycle 62): **SessionActionTool 612 (P0 phase 2 above)**, SqlDelightSessionStore 449, BusEvent.kt 449, FileProjectStore 405, ProviderQueryTool 403, SourceNodeActionTool 387, **TimelineActionTool 386 (down from cycle 52's 579 — cycle 52 transition handler extraction worked as designed)**, Agent 384, SessionQueryTool 378, ClipActionTool 377. None ≥ 800 LOC. **方向：** observational. **触发条件：** any top-10 file ≥ 800 LOC → forced P0 split. Rubric §5.6。Milestone §later.
- **debt-tool-spec-token-snapshot-2026-05-02-v6** — R.6 #1 audit @ 2026-05-02 (cycle 62): last measured 14,982 tokens / 38 audit-subset tools (cycle 36 baseline). Cycle 50 e99f2c7d added a runtime gauge (`agent.tool_spec_budget.tokens` exposed via `GET /metrics`) — next repopulate can read live values rather than re-running the audit test. Cycles 44 + 49 banked ~1.7k tokens; cycle 61 dispatcher add +helpText (~600 tokens) until phase 2 unregisters 4 underlying tools (~3-4× helpText token win). **方向：** observational. **触发条件：** runtime gauge > 15,500 OR audit tool count > 40 (excluding AIGC) → promote `debt-shrink-tool-spec-surface` to P0. Rubric §5.7。Milestone §later.
- **debt-project-field-snapshot-18-2026-05-02-v6** — R.5 #3 scan @ 2026-05-02 (cycle 62): `Project.kt` 顶层 `val` 字段数 = 18 (unchanged across cycles 31, 36, 42, 47, 52, 62 — 6 consecutive scans). **方向：** observational. **触发条件：** next repopulate field count > 18 AND 新增字段是 List<X> / Map<K, V> 类型. Rubric §5.6 / §3a-3。Milestone §later.
- **debt-bench-coverage-snapshot-9-2026-05-02-v1** — NEW R.6 #4 scan @ 2026-05-02 (cycle 62): 9 benchmark files exist in `core/src/jvmTest/kotlin/io/talevia/core/bench/` (Agent / Compaction / Export / FileProjectStore / Lockfile×2 / SourceDeepHash / ToolDispatch / BusEvent). Critical-path coverage: agent loop ✓ / compaction ✓ / ExportTool ✓ / FileProjectStore.openAt ✓ / Lockfile lookups ✓ / source DAG hash ✓. **方向：** observational. **触发条件：** new critical path lands without benchmark (e.g. server SSE handler / desktop dispatch path) OR benchmark coverage drops (file deleted) → promote a `debt-add-benchmark-<path>` bullet. Rubric §5.7 / §5.3。Milestone §later.
- **debt-ios-filter-parity-test** — iOS AVFoundation filter dispatch 单 gate (manifest only)。**方向：** add iOS-side test introspecting `AVFoundationVideoEngine` filter dispatch。**触发条件：** iOS Swift filter dispatch grows / shrinks OR iOS 平台窗口打开 OR real-world iOS render reports missing effect. Rubric 外 / 顺手记录。Milestone §later.
- **debt-split-bus-event** — `core/bus/BusEvent.kt` 449 LOC（cycle 35 5653cb99 加 `BusEvent.ToolStreamingPart` +48 LOC）。**方向：** flatten 23 nested sealed-type variants 到 top-level + rename ~40+ `is BusEvent.X` patterns。Rubric §5.6 / §3a-3。Milestone §later. · skipped 2026-05-02: trigger 条件未触发 — 449 < R.5 #4 500-LOC 阈值. Re-evaluate when BusEvent.kt ≥ 500 LOC.
- **re-evaluate-cross-engine-incremental-render** — Android + iOS per-clip render cache parity (FFmpeg JVM has it; M3 ExportTool dispatches `engine.supportsPerClipCache`)。**方向：** 用户决定 promote / demote / delete. Rubric §5.3 / §5.7。Milestone §later. · skipped 2026-05-02: 平台窗口仍未开 (user 仅 unblock desktop; Android/iOS 维持不退化底线).
- **re-evaluate-effect-kind-registry-cross-engine-tax** — 3-platform refactor without concrete next-effect-kind driver. **方向：** 用户决定 promote / demote / delete. Rubric §5.2 / §5.6。Milestone §later. · skipped 2026-04-25: meta bullet (driver-gated; 没有具体新 effect kind).
- **re-evaluate-desktop-merge-conflict-strategy** — `talevia merge` CLI + 3-way merge across timeline / source / lockfile. **方向：** 用户决定 promote / demote / delete. Rubric §5.4 / §3.4。Milestone §M8 (driver-gated). · skipped 2026-04-25: meta bullet (driver-gated; 没有真实多用户 conflict 报告).
- **debt-lockfile-lazy-interface-phase2b1c-3-bound-to-local** — Phase 2b-1c-3 sub-phase: 6 bound-to-local sites (`GcLockfileHandler.kt:36`, `PruneLockfileHandler.kt:29`, `LockfileEntriesQuery.kt:33`, `SpendQuery.kt:32`, `ArtifactTimeline.kt:58`, `SlashCommandDispatcher.kt:782`) all do `val entries = project.lockfile.entries` then mixed eager/lazy ops downstream. Per-call analysis: which need `List` (random access / `.size` / `[i]`) vs which can use `stream()` / `Sequence`. Migrate to whichever view their downstream needs; `entries: List<>` only kept on interface as escape hatch. **方向：** wait for phase 2b-1c-1-impl to ship; then walk each site, decide List vs Sequence, migrate. **触发条件：** phase 2b-1c-1-impl landed (cycle 59 ✓) AND a concrete LazyJsonlLockfile impl is being drafted (without that, this is purely cosmetic — `entries` already exposed as List works fine). Rubric §5.7 / §5.3。Milestone §later.
- **debt-evaluate-filter-action-future-home** — Cycle 56 refused filter→timeline_action absorption (Option D over the bullet's (a)/(b) framing). Filter is rendering-layer (params: Map<String,Float> + LUT bindings); timeline_action is structural (track / transition verbs). Cohesion-wise filter sits closer to clip_action's effect-on-clip semantics than timeline_action's verb set. ClipActionTool already has 12 verbs / 16+ nullable fields and is on R.5 #3 watch (`debt-split-clip-action-tool-input-seed`); merging filter in now would push past the 14-verb / 500-LOC trigger. **方向：** observational. **触发条件：** clip_action splits via sealed-Input phasing (mirroring session_action) AND the resulting sub-tool has a natural slot for `apply_filter` / `remove_filter` / `apply_filter_lut` semantically (e.g. a `clip_render_action` arm) → revisit absorption. Until then filter_action stays standalone at 261 LOC. Rubric §5.7 / §3a-1 / §5.6。Milestone §later.
- **debt-streaming-tool-output-non-aigc-tools** — NEW. `BusEvent.ToolStreamingPart` (cycle 35 5653cb99) added incremental tool output streaming, currently emitted only by `AigcSpeechGenerator` per cycle's chunk. Other long-running tools (`ExportTool` render with N-second progress, `RegenerateStaleClipsTool` per-clip dispatch loop, `ImportProjectFromJsonTool` rehome+validate phase) don't stream — agent + UI only see the final result. The infrastructure is ready (`emitPart` callback in `ToolContext`). **方向：** identify 2-3 long-running tools where streaming progress would visibly improve UX (export progress %, regenerate per-clip status, import rehome step), wire their progress signal through `emitPart(BusEvent.ToolStreamingPart(...))`. Each tool independently — not a structural change. **触发条件：** real user-report of "tool felt frozen" OR desktop chat panel UX work needs streaming surface OR M9 promotion (where multi-step progress matters). Rubric §5.4 / §5.7。Milestone §later.
- **debt-deep-content-hash-order-insensitive-claim-vs-impl** — Discovered cycle 172 while writing `DeepContentHashTest`. `core/domain/source/DeepContentHash.kt` kdoc claims "we intentionally normalise at the deep layer to make the deep hash order-insensitive over parents", but the implementation folds `node.contentHash` (shallow) into the result — and shallow IS order-sensitive over parents (hashes the serialised JSON list verbatim). So the deep hash also ends up order-sensitive, contradicting the kdoc. The parent-hash projection IS sorted by id at the join step (line 75), but that only stabilises the `parents=` portion of the fold; `shallow=${node.contentHash}|` in the same fold leaks order. Test `deepHashIsOrderSensitiveOverParentsBecauseShallowContributes` pins ACTUAL behavior. **方向：** decide whether to (a) fix the impl — drop `shallow=${node.contentHash}` from the deep fold and re-derive shallow from sorted parents, semantic change that may invalidate every cached deep hash; or (b) fix the kdoc — drop the order-insensitivity claim, keep impl unchanged, accept that re-ordering parents over-invalidates (safe direction). Rubric 外 / 顺手记录. Milestone §later.
- **debt-export-output-builder-text-clip-skipped-claim-vs-impl** — Discovered cycle 177 while writing `ExportToolOutputBuilderTest`. `core/tool/builtin/video/export/ExportToolOutputBuilder.kt` kdoc on `buildPerClipCostAttribution` claims "Empty inputs and clips without `assetId` (text clips) are skipped — they'd never have a lockfile entry anyway." But the impl unconditionally writes `perClip[clip.id.value] = cents` regardless of whether `assetId` is null — so text clips ARE in the map with null cents, NOT skipped. Test `textClipIsInMapWithNullCentsKdocMismatch` pins ACTUAL behavior. **方向：** decide whether to (a) fix the kdoc — drop "skipped" claim, accept that text clips show as "unpriced" (null cents), which actually matches the documented "agent can see 'unpriced' distinct from 0 cents" pattern for unpriced video clips so the kdoc's "skipped" wording is just inconsistent with itself; or (b) fix the impl — add `if (assetId == null) continue` to exclude text clips from map. Option (a) is the cheaper / safer fix and aligns with the existing per-clip-no-lockfile-entry case. Rubric 外 / 顺手记录. Milestone §later.
