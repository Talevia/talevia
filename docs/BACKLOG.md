# Backlog

`/iterate-gap` 的驱动源。每个 cycle 的流程：

1. 读这个文件，挑**最靠上的未完成**任务（P0 → P1 → P2，同档内按出现顺序）。
2. plan → 实现 → 验证 → 归档决策 → commit + push。
3. 在 iteration 的 commit 里顺手把本任务的 bullet 从这个文件里删掉（不是打钩，直接删），保持列表紧凑。
4. 如果这个文件的 P0/P1/P2 三档都已清空（或文件不存在），`/iterate-gap` 会按 skill 里的 rubric 分析一次性生成 **20 条新任务**写回这个文件、commit，然后**在同一个 cycle 里继续**挑新生成列表里第 1 条开干。

任务描述只给 **Gap（现状 / 痛点）** 和 **方向（期望动的东西）** —— 细节留给 cycle 里的 plan 阶段现读代码决定。Rubric 轴用 `§5.x` 标。

---

## P0 — 高杠杆、下一步就该动


## P1 — 中优，做完 P0 再排

- **debt-tool-consolidation-project-action-phase1** — Re-framed phase-1 of cycle 42's bullet (after the AIGC-arc parallel was identified in cycle 45 skip-tag). Phase 1 scope: introduce `project_action(kind=...)` as a new sealed-Input dispatcher consuming the existing 4 action tools (`project_action` / `project_maintenance_action` / `project_pin_action` / `project_snapshot_action`) — **the 4 tools STAY registered** (phase 2 unregisters them; tracks the AIGC arc precisely). Query stays separate (read vs write). The phase-1 dispatcher is pure addition; 0 file deletes, ~100 LOC new dispatcher file. Rubric §5.7 / §5.6 / §3a-1。Milestone §later.
- **debt-tool-consolidation-timeline-action-phase1b** — Phase 1b sub-bullet (depends on phase 1a). Absorb `filter_action`'s 3 verbs (`apply` / `remove` / `apply_lut`) into the renamed `timeline_action` — verbs become `apply_filter / remove_filter / apply_lut`. The FilterActionVerbs registry shape (object-based verb dispatch) is the unique complexity here; phase 1b decides whether the registry stays as-is (typealias-based dispatch) or unfolds into the flat `when` like the other domains. Rubric §5.7 / §5.6 / §3a-1。Milestone §later.
- **debt-shrink-tool-spec-surface** — Meta-gate for the per-area consolidation phases. R.6 #1 audit @ 2026-05-02: 14,982 tokens / 38 audit-subset tools (cycle 36 baseline). Cycle 44 absorbed `clip_set_action` into `clip_action` (saved ~1k tokens of `clip_set_action` tool spec); next saves come from `project_action` consolidation (≥1k target) + `timeline_action` consolidation (≥700 target). **方向：** observational gate — actionable work happens in the per-area phase-1 bullets above. **触发条件：** all three per-area phase-1 bullets done → re-measure tool_spec_budget; if reduction < 1.5k tokens cumulative, draft phase-2 bullets (tool body merging instead of just dispatcher consolidation). Rubric §5.7。Milestone §later.
- **perf-tool-spec-budget-runtime-metric** — `tool_spec_budget` is currently measured by an offline audit test (`EffectToolSpecBudgetTest` / `ToolSpecBudgetGateTest`); there's no `core.metrics` counter exposing the live per-turn token cost. R.6 #1's "next repopulate audit token > 15,500" trigger relies on manually re-running the audit. **方向：** add a `Metrics.toolSpecBudget` gauge that the agent populates at turn-prep time (when bundling tool specs into the LLM request); expose on `GET /metrics` (Prometheus scrape). Drives data-driven `debt-shrink-tool-spec-surface` decisions instead of cycle-cadence audits. Rubric §5.7。Milestone §later.
- **debt-split-clip-action-tool-input-seed** — Cycle 44's clip_action consolidation grew the file from 291 → 377 LOC (+86 LOC for 3 new verbs + 6 new data classes + 3 new result types). Now 12 verbs, ~16 nullable Input fields, ~13 Output result lists. Pattern matches the SessionActionTool 606-LOC trajectory (14 verbs, 17 nullable fields). **方向：** observational. **触发条件：** ClipActionTool.kt ≥ 500 LOC OR verb count > 14 → split via the same sealed-Input phasing as session_action. Rubric §5.6 / §3a-3。Milestone §later.
- **debt-clip-action-test-consolidation-seed** — Cycle 44 left 3 test files with the obsolete `ClipSetAction*` filename prefix (`ClipSetActionVolumeTest`, `ClipSetActionTransformTest`, `ClipSetActionSourceBindingTest`) — they exercise `clip_action(action="set_*")` now but the filenames pre-cycle-44. **方向：** rename file paths to `ClipActionVolumeTest` / `ClipActionTransformTest` / `ClipActionSourceBindingTest` (file rename via `git mv`); class names follow. Pure cosmetic rename; ~3 file renames + 3 class renames + 0 LOC body changes. Rubric §5.6。Milestone §later.

## P2 — 记债 / 观望

- **debt-tool-count-snapshot-56-2026-05-02-v4** — R.5 #1 + R.6 #1 scan refresh @ 2026-05-02 (cycle 47): `core/tool/builtin/**/*Tool.kt` count = 56 (was 57 cycle 42 baseline; **net -1** because cycle 44 dacc7daf absorbed `ClipSetActionTool` into `clip_action(action=set_*)`). **方向：** observational. **触发条件：** next repopulate count > 56 OR 单领域出现 ≥ 3 同前缀 prefix-group → promote `debt-shrink-tool-spec-surface` 到 P0/P1. Rubric §5.6。Milestone §later.
- **debt-todo-snapshot-0-2026-05-02-v4** — R.5 #6 anchored-regex scan @ 2026-05-02 (cycle 47): 0 code-comment-form TODO/FIXME/HACK/XXX in `core/commonMain` (unchanged from cycles 31, 36, 42). **方向：** observational. **触发条件：** next repopulate anchored-regex count > 0 → real `// TODO:` debt landed. Rubric §5.6。Milestone §later.
- **debt-long-file-snapshot-2026-05-02-v6** — R.5 #4 scan refresh @ 2026-05-02 (cycle 47): SessionActionTool 606 (P0 phase 1a split bullet active above), SqlDelightSessionStore 449, BusEvent.kt 449, FileProjectStore 405, ProviderQueryTool 403, SourceNodeActionTool 387, Agent 384, SessionQueryTool 378, **ClipActionTool 377 (NEW since cycle 42; +86 LOC from cycle 44 absorption — see `debt-split-clip-action-tool-input-seed` P1 above)**, TransitionActionTool 360, ImportMediaTool 358. None ≥ 500 LOC. **方向：** observational. **触发条件：** any top-10 file ≥ 800 LOC → forced P0 split. Rubric §5.6。Milestone §later.
- **debt-tool-spec-token-snapshot-2026-05-02-v4** — R.6 #1 audit @ 2026-05-02 (cycle 47): last measured 14,982 tokens / 38 audit-subset tools (cycle 36 baseline; not re-measured this cycle pending the `perf-tool-spec-budget-runtime-metric` P1 work above). **方向：** observational. **触发条件：** next repopulate audit token > 15,500 OR audit tool count > 40 (excluding AIGC) → promote `debt-shrink-tool-spec-surface` to P0. Rubric §5.7。Milestone §later.
- **debt-project-field-snapshot-18-2026-05-02-v4** — R.5 #3 scan @ 2026-05-02 (cycle 47): `Project.kt` 顶层 `val` 字段数 = 18 (unchanged from cycles 31, 36, 42). **方向：** observational. **触发条件：** next repopulate field count > 18 AND 新增字段是 List<X> / Map<K, V> 类型. Rubric §5.6 / §3a-3。Milestone §later.
- **debt-unified-dispatcher-select-plugin-shape** — `project_query` 现已 22 selects；`rejectIncompatibleProjectQueryFilters` 已 24 规则；dispatcher 仍是单文件 monolith。**方向：** 当 select 数 ≥ 25 OR 规则 ≥ 30 时实施 select-plugin registry 或按主题拆 dispatcher。Rubric 外 / 顺手记录。Milestone §later.
- **debt-ios-filter-parity-test** — iOS AVFoundation filter dispatch 单 gate (manifest only)。**方向：** add iOS-side test introspecting `AVFoundationVideoEngine` filter dispatch。**触发条件：** iOS Swift filter dispatch grows / shrinks OR iOS 平台窗口打开 OR real-world iOS render reports missing effect. Rubric 外 / 顺手记录。Milestone §later.
- **m9-binding-conflict-detection-seed** — VISION §3.3 / M9-candidate. Multi-binding clips 当前 blind-concat-folded in `AigcPipeline.foldPrompt(...)`. **方向：** static `project_query(select=binding_conflicts)` scan over each clip's bound source nodes. **触发条件：** M9 promotion (§4 GUI mostly done) OR real user-report. Rubric §3.3 / §5.5。Milestone §later.
- **debt-split-bus-event** — `core/bus/BusEvent.kt` 449 LOC（cycle 35 5653cb99 加 `BusEvent.ToolStreamingPart` +48 LOC）。**方向：** flatten 23 nested sealed-type variants 到 top-level + rename ~40+ `is BusEvent.X` patterns。Rubric §5.6 / §3a-3。Milestone §later. · skipped 2026-05-02: trigger 条件未触发 — 449 < R.5 #4 500-LOC 阈值. Re-evaluate when BusEvent.kt ≥ 500 LOC.
- **re-evaluate-cross-engine-incremental-render** — Android + iOS per-clip render cache parity (FFmpeg JVM has it; M3 ExportTool dispatches `engine.supportsPerClipCache`)。**方向：** 用户决定 promote / demote / delete. Rubric §5.3 / §5.7。Milestone §later. · skipped 2026-05-02: 平台窗口仍未开 (user 仅 unblock desktop; Android/iOS 维持不退化底线).
- **re-evaluate-effect-kind-registry-cross-engine-tax** — 3-platform refactor without concrete next-effect-kind driver. **方向：** 用户决定 promote / demote / delete. Rubric §5.2 / §5.6。Milestone §later. · skipped 2026-04-25: meta bullet (driver-gated; 没有具体新 effect kind).
- **re-evaluate-desktop-merge-conflict-strategy** — `talevia merge` CLI + 3-way merge across timeline / source / lockfile. **方向：** 用户决定 promote / demote / delete. Rubric §5.4 / §3.4。Milestone §M8 (driver-gated). · skipped 2026-04-25: meta bullet (driver-gated; 没有真实多用户 conflict 报告).
