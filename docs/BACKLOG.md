# Backlog

`/iterate-gap` 的驱动源。每个 cycle 的流程：

1. 读这个文件，挑**最靠上的未完成**任务（P0 → P1 → P2，同档内按出现顺序）。
2. plan → 实现 → 验证 → 归档决策 → commit + push。
3. 在 iteration 的 commit 里顺手把本任务的 bullet 从这个文件里删掉（不是打钩，直接删），保持列表紧凑。
4. 如果这个文件的 P0/P1/P2 三档都已清空（或文件不存在），`/iterate-gap` 会按 skill 里的 rubric 分析一次性生成 **20 条新任务**写回这个文件、commit，然后**在同一个 cycle 里继续**挑新生成列表里第 1 条开干。

任务描述只给 **Gap（现状 / 痛点）** 和 **方向（期望动的东西）** —— 细节留给 cycle 里的 plan 阶段现读代码决定。Rubric 轴用 `§5.x` 标。

---

## P0 — 高杠杆、下一步就该动

- **re-evaluate-debt-aigc-tool-consolidation** — `debt-aigc-tool-consolidation` 跨 9+ repopulate cycles 持续 skip-tagged（自 cycle 113 起，原因 §3a-7：`LockfileEntry.toolId` 已 stamped `"generate_image"` 等在 on-disk bundle，删旧工具会让 `ReplayLockfileTool` 对老 entry 失效）。**方向：** 用户决定 promote (设计 alias-map: `"generate_image" → "aigc_generate" + kind="image"` 写进 ReplayLockfileTool) / demote (接受 4 个独立工具的 LLM context tax) / delete (criterion 不再相关)。Rubric §5.6 / §5.7 / §3a #1 / §3a #7。Milestone §later. · skipped 2026-04-25: meta bullet awaiting user decision (promote / demote / delete).

## P1 — 中优，做完 P0 再排

- **debt-action-tool-dispatcher-shape** — Architecture-tax mirror of cycle-154's `QuerySelect` work for the 8 `*ActionTool` dispatchers (ProjectActionTool, SessionActionTool, SourceNodeActionTool, TrackActionTool, FilterActionTool, ClipActionTool, ClipSetActionTool, ProjectMaintenanceActionTool, ProjectPinActionTool, ProjectSnapshotActionTool). Each has an inline `execute() { when (input.action) { ... } }` arm-tree; adding a verb edits the central dispatcher in 2-4 places (when arm + helpText fragment + schema enum + sometimes Input field). **方向：** define `core.tool.action.ActionVerb<I, O, C>` interface (per-verb object bundling action id + handler), mirror of `QuerySelect<I, O, C>`; migrate ONE smallest dispatcher (FilterActionTool, 3 verbs) as proof-of-concept. Document migration recipe for the other 7 in commit body so a future cycle can take next chunk. Rubric §5.6 / §3a #12。Milestone §later.
- **debt-split-session-action-tool-input** — SessionActionTool.kt 606 LOC, with `Input` carrying 17+ optional fields (one per action's params). The "every action gets its own optional Input field" pattern is the LOC driver: each new verb adds 5-15 LOC to the Input data class + matching schema property. The `*ActionToolSchema.kt` extract (cycle 150) addressed schema-side bloat; Input/Output remain in the dispatcher. **方向：** consider splitting Input into sealed polymorphic shapes per-action (`SessionActionTool.ArchiveInput / RenameInput / DeleteInput / ...`) using kotlinx.serialization's `JsonClassDiscriminator`. Type-safe per-action contract, zero "this field belongs to that verb but applies to none" ambiguity. Trade-off: caller needs to construct per-verb input class (no longer the loose `Input(action="…", newTitle="…")` shape). §3a #7 forward-compat: migration path needs default-discriminator fallback so old JSON decodes. Rubric §5.6 / §3a-3。Milestone §later.
- **debt-runtime-test-export-tool-incremental** — ExportTool's per-clip incremental render path (`runPerClipRender`) has handler-level unit tests but no end-to-end runtime test that exercises mezzanine fingerprint compute → cache hit → concat-demuxer fast-path → bit-identical output. Critical-path runtime coverage (R.5 #10) — the path's correctness depends on `clipMezzanineFingerprint` matching between writes/reads and on `concatMezzanines` producing byte-deterministic output. **方向：** add `:platform-impls:video-ffmpeg-jvm:test` end-to-end test: render a 3-clip timeline twice, second render should report `perClipCacheHits=3 / Misses=0` and produce byte-identical bytes to the first. Validates the entire cycle: mezzanine write → fingerprint match → concat → output. Rubric §5.6 / R.5 #10。Milestone §later.
- **re-evaluate-lockfile-incremental-load** — `lockfile-incremental-load` 跨 4+ repopulate cycles (122/128/134/154/160) 持续 skip-tagged with the same scope reason: multi-file write atomicity (envelope ↔ lockfile.jsonl tx) needs explicit user signoff on bundle-inconsistency-on-crash trade-off; no real-world > 500-entry project to validate against `FileProjectStoreOpenAtBenchmark`. **方向：** 用户决定 promote (define the atomicity contract — fsync order or two-phase write or accept best-effort), demote (acknowledge O(N) lockfile re-decode is acceptable until N really hurts), or delete (lockfile is unlikely to grow large in practice). Rubric §5.7 / §5.3 / §3a-3。Milestone §later. · skipped 2026-04-25: meta bullet (was P0; converted to re-evaluate after 3+ skip cycles).
- **re-evaluate-aigc-result-multi-variant** — `aigc-result-multi-variant` 跨 4+ repopulate cycles persistently skip-tagged: §3a #4 (states should not be binary) + design ambiguity — bullet's "shared inputHash" semantic conflicts with seed-in-hash invariant (different seeds → different hashes), AND video providers (Sora 2 / Veo) don't natively support `n` so per-variant calls have separate-billing UX implications. **方向：** 用户决定 promote (specify the variant-key shape — variantIndex as a separate column not a hash override, per-variant billing UX baseline), demote (single-variant is fine until users ask), or delete. Rubric §5.2。Milestone §later. · skipped 2026-04-25: meta bullet (was P1; converted after 3+ skip cycles).
- **re-evaluate-effect-kind-registry-cross-engine-tax** — `effect-kind-registry-cross-engine-tax` 跨 4+ repopulate cycles skip-tagged: scope — 3-platform refactor without a concrete new effect kind to validate the registry against. **方向：** 用户决定 promote (next new effect kind addition pulls the registry refactor along as a prerequisite), demote (4 aligned kinds work fine via existing test coverage; defer indefinitely), or delete (assume cross-engine alignment will always be hand-coded). Rubric §5.2 / §5.6。Milestone §later. · skipped 2026-04-25: meta bullet (was P1; converted after 3+ skip cycles).
- **re-evaluate-desktop-merge-conflict-strategy** — `desktop-merge-conflict-strategy` 跨 4+ repopulate cycles skip-tagged: scope — new top-level `talevia merge` CLI command with semantic 3-way merge across timeline / source / lockfile is multi-cycle work; no real-world multi-user bundle conflict reported yet. **方向：** 用户决定 promote (commit to multi-cycle work; pick first slice — `.gitattributes` `merge=talevia-json` driver as MVP), demote (accept manual JSON merge until users complain), or delete. Rubric §5.4 / §3.4。Milestone §later. · skipped 2026-04-25: meta bullet (was P1; converted after 3+ skip cycles).
- **re-evaluate-m2-provider-second-impl** — M2 exit criterion 2 (provider 多元) is the **only blocker** keeping M2 at 6/7 done; cross 12+ repopulate cycles skip-tagged for "需专有 API key + vendor 决策". **方向：** 用户决定 promote (vendor + key — e.g. OpenAI DALL-E for ImageGen, ElevenLabs for TTS, Anthropic for vision), demote (M2 exit criterion 2 reworded to "single provider acceptable"), or delete (criterion not relevant). M2 auto-promote is structurally locked until this resolves. Rubric §5.7 / §5.2。Milestone §M2. · skipped 2026-04-25: meta bullet awaiting user decision (no agent action possible).
- **cross-engine-incremental-render-android** — CLAUDE.md "Known incomplete" calls out: per-clip incremental render (mezzanine + concat-demuxer fast-path) is FFmpeg-only; Media3VideoEngine on Android has `supportsPerClipCache=false` so `ExportTool` always falls through to `runWholeTimelineRender`. Long Android exports re-encode every clip every time. **方向：** implement `Media3VideoEngine.renderClip` + `concatMezzanines` using Media3's `Transformer` API with bitexact flags; flip `supportsPerClipCache=true`; add Android-side mezzanine cache directory (or share `<bundle>/.talevia-cache/` since per-engine fingerprint already separates). Validate via existing `:apps:android:assembleDebug` + a new ExportToolBenchmark Android-platform variant. Rubric §5.3 / §5.7。Milestone §later. · skipped 2026-04-25: CLAUDE.md "Platform priority — 当前阶段" stance — iOS / Android 维持不退化, 不主动扩新特性; needs concrete user driver before this becomes valid.
- **cross-engine-incremental-render-ios** — Same Known-incomplete as above, AVFoundation engine. AVAssetExportSession + AVMutableComposition can stitch pre-rendered mezzanines (concat path) but per-clip render needs AVAssetWriter discipline for bitexact output. **方向：** implement `AVFoundationVideoEngine.__renderClip` + `__concatMezzanines` matching the FFmpeg engine's bitexact contract; flip `supportsPerClipCache=true`; add iOS-side mezzanine cache. Validate via xcodebuild + `:core:linkDebugFrameworkIosSimulatorArm64`. Rubric §5.3 / §5.7。Milestone §later. · skipped 2026-04-25: same CLAUDE.md "Platform priority" stance as android sibling — iOS 维持不退化, 不主动扩新特性; needs concrete user driver.

## P2 — 记债 / 观望

- **debt-todo-fixme-baseline-31** — R.5.6 scan: 31 TODO/FIXME/HACK occurrences in core/commonMain (unchanged from cycle 154 baseline of 31). **方向：** observational; trigger watches for *increases* (the count drift is asymmetric — a slow ratchet down is healthy). **触发条件：** next repopulate count > 31. Rubric §5.6。Milestone §later.
- **debt-tool-count-watch-60** — R.5.1 scan 60 tools (unchanged from cycle 153/154 — `debt-shrink-tool-spec-surface` umbrella's ≤ 60 cap target still held). **方向：** observational; if count climbs back above 60, next repopulate should re-promote `debt-shrink-tool-spec-surface` to P0/P1. **触发条件：** next repopulate count > 60. Rubric §5.6。Milestone §later.
- **debt-long-file-creep-watch-606** — Cycle 160 longest files: SessionActionTool 606 LOC (cycle-158 helpText trim 622 → 606), TrackActionTool 525, FilterActionTool 502, SqlDelightSessionStore 449 (was 387; cycle-155 maxMessages cap added countBySession + ctor param), ProviderQueryTool 403, SessionQueryTool 378, SourceNodeActionTool 396, FileProjectStore 383, SynthesizeSpeechTool 369, GenerateVideoTool 369, BusEvent 345 (cycle-159 kdoc trim 491 → 345). Three over §3a-3 500-LOC threshold; SqlDelightSessionStore approaching it. All under R.5.4's 800-LOC strong-P0 line. **方向：** observational; SqlDelightSessionStore +51 LOC away from 500 (next significant addition trips the watch); SessionActionTool's next phase remains Input/Output extract via `internal typealias`. **触发条件：** any of SessionActionTool / TrackActionTool / FilterActionTool / SqlDelightSessionStore approaches 700 LOC, or ProviderQueryTool / SessionQueryTool / BusEvent approaches 480 LOC. Rubric §5.6。Milestone §later.
- **debt-sqldelightsessionstore-creep-449** — `SqlDelightSessionStore.kt` 449 LOC, +62 since cycle 155 added `maxMessages` cap + `countBySession` query + ctor param. Approaching §3a-3 500-LOC threshold (51 LOC headroom). **方向：** observational; first signal of the per-session bookkeeping (cap, count queries, recorder wires) accumulating. Next extension that needs another count / threshold would push past 500 and trip `debt-long-file-creep-watch-606`'s SqlDelightSessionStore-specific clause. Plausible split: extract message / part / permission queries into per-domain handler functions in sibling files, mirroring the cycle-150 SessionActionToolSchema split shape. **触发条件：** SqlDelightSessionStore reaches 500 LOC. Rubric §5.6 / §3a-3。Milestone §later.
- **desktop-live-render-preview** — Desktop only shows `Part.RenderProgress.Preview` JPEG ticks if engine emits them; no continuous playback surface. Compose Desktop has Skia / VLC / mpv bridges that could drive a real `mediaPlayer` widget. **方向：** Compose Desktop + Media3 / FFmpeg playback engine bridges drive a preview surface; subscribe `BusEvent.AigcJobProgress` + `Part.RenderProgress.Preview` to swap clip thumbnails inline when AIGC products land. **触发条件：** Desktop reaches CLI parity per CLAUDE.md "Platform priority" (currently "iOS / Android non-regression only" — Desktop window not yet open). Rubric §5.4 / desktop。Milestone §later.
- **bundle-talevia-json-split** — `talevia.json` carries timeline + assets + source DAG + lockfile + snapshots + per-Project metadata in one envelope. The P1 `re-evaluate-lockfile-incremental-load` is one slice; this bullet covers follow-ups (snapshots into `snapshots/` subdir; large per-snapshot bodies don't bloat the per-mutate re-encode). **方向：** after lockfile gets resolved (P1), evaluate snapshots; trigger likely needs real-world > 1 MB snapshot bodies or git-diff-noise complaints. **触发条件：** real user diff-noise report OR snapshot ≥ 1 MB OR lockfile-incremental-load ships and exposes snapshots as the new bottleneck. Rubric §3a-3 / §5.7。Milestone §later.
- **agent-tool-streaming-text-delta** — When Talevia eventually adds a tool that emits textual / byte-stream deltas (shell exec, log-tail, chunked file read, web search streaming), it'll need a generic `BusEvent.ToolStreamingPart(callId, partId?, deltaText?, sizeBytes?, message?)`. Today no such tool exists: AIGC uses lifecycle-tick `BusEvent.AigcJobProgress`, FFmpeg/Export uses per-frame `Part.RenderProgress`, ReadFileTool is synchronous. **方向：** ship the BusEvent + EventBusMetricsSink + ServerDtos exhaustive `when` arm together with the first chunk-emitting tool. **触发条件：** ≥ 1 chunk-emitting tool lands. Rubric §5.4。Milestone §later.
