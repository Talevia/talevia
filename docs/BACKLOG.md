# Backlog

`/iterate-gap` 的驱动源。每个 cycle 的流程：

1. 读这个文件，挑**最靠上的未完成**任务（P0 → P1 → P2，同档内按出现顺序）。
2. plan → 实现 → 验证 → 归档决策 → commit + push。
3. 在 iteration 的 commit 里顺手把本任务的 bullet 从这个文件里删掉（不是打钩，直接删），保持列表紧凑。
4. 如果这个文件的 P0/P1/P2 三档都已清空（或文件不存在），`/iterate-gap` 会按 skill 里的 rubric 分析一次性生成 **20 条新任务**写回这个文件、commit，然后**在同一个 cycle 里继续**挑新生成列表里第 1 条开干。

任务描述只给 **Gap（现状 / 痛点）** 和 **方向（期望动的东西）** —— 细节留给 cycle 里的 plan 阶段现读代码决定。Rubric 轴用 `§5.x` 标。

---

## P0 — 高杠杆、下一步就该动

- **debt-split-session-action-tool-input-phase1** — Sub-bullet 1/3 of the long-deferred `debt-split-session-action-tool-input` (4 cycles skip-tagged). `SessionActionTool.kt` 606 LOC, single `Input` data class with 17 optional fields covering 14 verbs. Phase 1 scope: introduce `sealed class Input` with `@JsonClassDiscriminator("action")` + 14 per-verb data classes (`ArchiveInput / RenameInput / DeleteInput / ForkInput / RevertInput / CompactInput / ListInput / ...`); rewrite the dispatcher's `when (input.action)` to a sealed-type `when (input) is Input.Archive ->` form. **Do not** touch handler-fn signatures yet (they still take typed `SessionActionTool.Input` — phase 2 carves them out). Drop the legacy 17-field `Input` class. Phase 1 is independently shippable: tests still construct verb-specific subclasses, JSON schema becomes `oneOf` automatically. Rubric §5.6 / §3a-3。Milestone §later. · skipped 2026-05-02: phase-1 boundary unbuildable as drawn — handler bodies access `input.<field>` which doesn't exist on the sealed parent, forcing one of two non-phase-1 paths: (a) handler signatures change to `(Input.X, ...)` typed (phase 2's scope); (b) inline `(input as Input.X).<field>` cast at top of every handler body (~14 casts). Either way, all 13 test files break compile because `Input(action="archive", sessionId="...")` ctor is gone — they MUST be migrated in same cycle to keep gradle green. Net true scope of "phase 1" = sealed Input + dispatcher + handler casts/typed-args + 13 test files = ~26 files / ~850 LOC, indistinguishable from the original big-bang. Re-phase: phase 1a = extract Input only (introduce sealed alongside, keep flat as deprecated alias for 1 cycle); phase 1b = migrate dispatcher + handlers; phase 1c = migrate tests + drop deprecated alias. Each is independently shippable.
- **debt-lockfile-lazy-interface-phase2b1** — Sub-bullet 1/3 of `debt-lockfile-lazy-interface-O1-open` (4 cycles skip-tagged). Introduce `interface Lockfile { fun stream(): Sequence<LockfileEntry>; fun findByInputHash(...): LockfileEntry?; ... }` alongside the existing data class. Provide a single `EagerLockfile` impl that wraps the current `entries: List<LockfileEntry>` and forwards every interface method; **all 30+ existing callers continue to work unchanged** (they still see `Lockfile` field on Project, only the type is the interface now, value is `EagerLockfile`). Phase 2b-2 (next bullet) does the caller migration to stream/lookup-by-key APIs. Phase 1 is pure addition. Rubric §5.7 / §5.3 / §3a-3。Milestone §later. · skipped 2026-05-02: "pure addition" framing breaks down on construction sites — 72 `Lockfile(entries = ...)` ctor calls across prod + tests would need to become `EagerLockfile(...)` if `Lockfile` is renamed to interface, OR a `companion object operator fun invoke` factory hack that complicates kotlinx polymorphic serialization. Re-phase: phase 2b-1a = add `stream()` / `findByInputHash()` / `findByAssetId()` as MEMBER methods directly on the data class (truly pure addition, ~10 LOC, no construction-site impact); phase 2b-1b = migrate 30+ callers to the new methods; phase 2b-1c = extract data class to interface + EagerLockfile impl after all callers use the methods. Each independently shippable.
- **debt-tool-consolidation-project-action-phase1** — 5 same-prefix tools `project_action` / `project_maintenance_action` / `project_pin_action` / `project_query` / `project_snapshot_action` collectively contribute ~3.5k tokens of LLM context per turn; mirror `aigc_generate(kind=...)`'s phase 2 win by introducing `project_action(kind=...)` as a `sealed Input` dispatcher consuming the existing 4 action tools (query stays separate — read vs write). **方向：** sealed `Input` with `@JsonClassDiscriminator("kind")` + 4 verb-specific data classes; the dispatcher routes per kind to the existing tool's body (extract bodies into bespoke generators à la AIGC phase 3). Same-cycle Tool<I,O>-shell drop possible since the 4 underlying classes will only be reached via dispatcher. Target audit-subset reduction ≥ 1k tokens. Rubric §5.7 / §5.6 / §3a-1。Milestone §later. · skipped 2026-05-02: scope mirrors the AIGC consolidation arc which took 7 cycles (23 → 41) for phases 1 → 2 → 3a → 3b → 3c-1 → 3c-2 → 3c-3. Single-cycle "phase 1" boundary doesn't exist — sealed Input + dispatcher + 4 underlying-tool migrations + 4-tool-deregistration + ~15 test files ≈ 40 files / ~1.5k LOC. Re-phase along AIGC arc: phase 1 = sealed Input + dispatcher only (4 tools STILL registered); phase 2 = unregister 4 underlying tools; phase 3a/b/c = adapter family + Tool<I,O> drop + rename. Add per-phase sub-bullets at next repopulate.

## P1 — 中优，做完 P0 再排

- **debt-tool-consolidation-timeline-action-phase1** — Sibling consolidation for `track_action` + `transition_action` + `filter_action` (3 same-prefix tools, ~2.5k tokens). Merge into `timeline_action(kind=)` with sealed `Input`. **方向：** sealed `Input` discriminated by `kind` ∈ {add_track, remove_track, set_track, add_transition, set_transition, set_filter, ...}; underlying logic stays in domain extension methods. Target audit-subset reduction ≥ 700 tokens. Rubric §5.7 / §5.6 / §3a-1。Milestone §later. · skipped 2026-05-02: scope ≈ 27 files / ~900 LOC across 3 domains (Track 4 verbs / Transition 2 verbs / Filter 3 verbs = 9 disambiguated verbs total) + new tool id (`timeline_action`) breaking 3 prior tool-id wire-formats. Cycle 44's clip_action precedent absorbed 1 tool in 19 files; this is 2× scope with an additional rename arm. Re-phase: phase 1a = absorb `transition_action` into a renamed `track_action` → `timeline_action` (smallest sibling first; ~12 files); phase 1b = absorb `filter_action` (the FilterActionVerbs registry has its own dispatch shape — separate cycle); phase 1c = audit + drop legacy id deprecation aliases. Each independently shippable.
- **debt-shrink-tool-spec-surface** — Meta-gate for the per-area consolidation phases. R.6 #1 audit @ 2026-05-02: 14,982 tokens / 38 audit-subset tools (last measured cycle 36; tool count net-decreased to 57 after phase 3c-3 dropped 4 Tool<I,O> shells). Real ≥1k savings only achievable via the per-area consolidations above (`project_action` / `clip_action` / `timeline_action`); after all three land, re-audit and confirm. **方向：** observational gate — actionable work happens in the per-area phase-1 bullets above. **触发条件：** all three per-area phase-1 bullets done → re-measure tool_spec_budget; if reduction < 1k tokens, draft phase-2 bullets (tool body merging instead of just dispatcher consolidation). Rubric §5.7。Milestone §later.
- **session-project-binding-seed** — Multiple tools currently take `projectId: String?` in their Input; downstream they call `ctx.resolveProjectId(input.projectId)` which checks input first, falls back to `ToolContext.currentProjectId`. The session itself doesn't yet have a persistent `boundProjectId` — every turn must re-supply context. §3a #6 keeps flagging "session-project-binding 落地后这个参数应当从 session context 拿". **方向：** add `Session.currentProjectId: ProjectId?` field (nullable, default null, forward-compat); add `bind_session_project` tool to set it; ToolContext propagates from Session. Existing tools' `projectId` Input field stays (backward-compat), but the dispatcher prefers session.currentProjectId when input.projectId is null. Rubric §5.6 / §3a-6。Milestone §later.

## P2 — 记债 / 观望

- **debt-tool-count-snapshot-57-2026-05-02-v3** — R.5 #1 + R.6 #1 scan refresh @ 2026-05-02 (cycle 42): `core/tool/builtin/**/*Tool.kt` count = 57 (was 61 cycle 36 baseline; **net -4** because phase 3c-3 cycle 41 0130c908 dropped Generate*Tool / SynthesizeSpeechTool — they're now `Aigc*Generator` non-Tool classes). **方向：** observational. **触发条件：** next repopulate count > 57 OR 单领域出现 ≥ 3 同前缀 prefix-group → promote `debt-shrink-tool-spec-surface` 到 P0/P1。Rubric §5.6。Milestone §later.
- **debt-todo-snapshot-0-2026-05-02-v3** — R.5 #6 anchored-regex scan @ 2026-05-02 (cycle 42): 0 code-comment-form TODO/FIXME/HACK/XXX in `core/commonMain` (unchanged from cycles 31, 36). **方向：** observational. **触发条件：** next repopulate anchored-regex count > 0 → real `// TODO:` debt landed. Rubric §5.6。Milestone §later.
- **debt-long-file-snapshot-2026-05-02-v5** — R.5 #4 scan refresh @ 2026-05-02 (cycle 42): SessionActionTool 606 (P0 phase 1 split bullet active above), SqlDelightSessionStore 449, BusEvent.kt 449, FileProjectStore 405, ProviderQueryTool 403, SourceNodeActionTool 387, Agent 384, SessionQueryTool 378, TransitionActionTool 360, ImportMediaTool 358, AigcPipeline 358. Cycle 41 phase 3c-3 didn't materially change LOC (rename only). **方向：** observational. **触发条件：** any top-10 file ≥ 800 LOC → forced P0 split. Rubric §5.6。Milestone §later.
- **debt-tool-spec-token-snapshot-2026-05-02-v3** — R.6 #1 audit @ 2026-05-02 (cycle 42): last measured 14,982 tokens / 38 audit-subset tools (cycle 36 baseline; not re-measured this cycle since phase 3c work didn't touch LLM-facing tool surface). **方向：** observational. **触发条件：** next repopulate audit token > 15,500 OR audit tool count > 40 (excluding AIGC) → promote `debt-shrink-tool-spec-surface` to P0. Rubric §5.7。Milestone §later.
- **debt-project-field-snapshot-18-2026-05-02-v3** — R.5 #3 scan @ 2026-05-02 (cycle 42): `Project.kt` 顶层 `val` 字段数 = 18 (unchanged from cycles 31, 36). **方向：** observational. **触发条件：** next repopulate field count > 18 AND 新增字段是 List<X> / Map<K, V> 类型. Rubric §5.6 / §3a-3。Milestone §later.
- **debt-unified-dispatcher-select-plugin-shape** — `project_query` 现已 22 selects；`rejectIncompatibleProjectQueryFilters` 已 24 规则；dispatcher 仍是单文件 monolith。**方向：** 当 select 数 ≥ 25 OR 规则 ≥ 30 时实施 select-plugin registry 或按主题拆 dispatcher。Rubric 外 / 顺手记录。Milestone §later.
- **debt-ios-filter-parity-test** — iOS AVFoundation filter dispatch 单 gate (manifest only)。**方向：** add iOS-side test introspecting `AVFoundationVideoEngine` filter dispatch。**触发条件：** iOS Swift filter dispatch grows / shrinks OR iOS 平台窗口打开 OR real-world iOS render reports missing effect. Rubric 外 / 顺手记录。Milestone §later.
- **m9-binding-conflict-detection-seed** — VISION §3.3 / M9-candidate. Multi-binding clips 当前 blind-concat-folded in `AigcPipeline.foldPrompt(...)`. **方向：** static `project_query(select=binding_conflicts)` scan over each clip's bound source nodes. **触发条件：** M9 promotion (§4 GUI mostly done) OR real user-report. Rubric §3.3 / §5.5。Milestone §later.
- **debt-split-bus-event** — `core/bus/BusEvent.kt` 449 LOC（cycle 35 5653cb99 加 `BusEvent.ToolStreamingPart` +48 LOC）。**方向：** flatten 23 nested sealed-type variants 到 top-level + rename ~40+ `is BusEvent.X` patterns。Rubric §5.6 / §3a-3。Milestone §later. · skipped 2026-05-02: trigger 条件未触发 — 449 < R.5 #4 500-LOC 阈值. Re-evaluate when BusEvent.kt ≥ 500 LOC.
- **re-evaluate-cross-engine-incremental-render** — Android + iOS per-clip render cache parity (FFmpeg JVM has it; M3 ExportTool dispatches `engine.supportsPerClipCache`)。**方向：** 用户决定 promote / demote / delete. Rubric §5.3 / §5.7。Milestone §later. · skipped 2026-05-02: 平台窗口仍未开 (user 仅 unblock desktop; Android/iOS 维持不退化底线).
- **re-evaluate-effect-kind-registry-cross-engine-tax** — 3-platform refactor without concrete next-effect-kind driver. **方向：** 用户决定 promote / demote / delete. Rubric §5.2 / §5.6。Milestone §later. · skipped 2026-04-25: meta bullet (driver-gated; 没有具体新 effect kind).
- **re-evaluate-desktop-merge-conflict-strategy** — `talevia merge` CLI + 3-way merge across timeline / source / lockfile. **方向：** 用户决定 promote / demote / delete. Rubric §5.4 / §3.4。Milestone §M8 (driver-gated). · skipped 2026-04-25: meta bullet (driver-gated; 没有真实多用户 conflict 报告).
