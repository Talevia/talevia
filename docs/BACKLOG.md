# Backlog

`/iterate-gap` 的驱动源。每个 cycle 的流程：

1. 读这个文件，挑**最靠上的未完成**任务（P0 → P1 → P2，同档内按出现顺序）。
2. plan → 实现 → 验证 → 归档决策 → commit + push。
3. 在 iteration 的 commit 里顺手把本任务的 bullet 从这个文件里删掉（不是打钩，直接删），保持列表紧凑。
4. 如果这个文件的 P0/P1/P2 三档都已清空（或文件不存在），`/iterate-gap` 会按 skill 里的 rubric 分析一次性生成 **20 条新任务**写回这个文件、commit，然后**在同一个 cycle 里继续**挑新生成列表里第 1 条开干。

任务描述只给 **Gap（现状 / 痛点）** 和 **方向（期望动的东西）** —— 细节留给 cycle 里的 plan 阶段现读代码决定。Rubric 轴用 `§5.x` 标。

---

## P0 — 高杠杆、下一步就该动

- **debt-split-session-action-tool-input** — `SessionActionTool.kt` 606 LOC, `Input` 17 个 optional 字段。User authorization 2026-05-02 解除 big-bang 风险卡点 — 不要兼容性。**方向：** sealed `Input` + 每 verb 一个 data class（14 个：`ArchiveInput / RenameInput / DeleteInput / ForkInput / RevertInput / CompactInput / ListInput` 等）；用 kotlinx.serialization `@JsonClassDiscriminator("action")` 让 LLM JSON Schema 用 oneOf；一次性 big-bang refactor — handler signatures 改 + 13 test files 同 cycle 内更新；破坏 `Part.Tool.input` 已持久化 JSON (旧 session 无法 decode — acceptable)。Rubric §5.6 / §3a-3。Milestone §later. · skipped 2026-05-02: scope assessed mid-dispatch as ~2000 LOC mechanical change across 30+ files (sealed Input + 11 handler signatures + 13 test files + CLI usage + oneOf schema). 单 cycle big-bang 风险高 (mid-refactor partial-state commit / 测试串扰). Needs explicit phasing into sub-bullets matching cycle 22 cc556235 precedent (phase 1: sealed Input + dispatcher only / phase 2: handler signatures / phase 3: test migration); next repopulate or user decision splits.

## P1 — 中优，做完 P0 再排

- **aigc-tool-consolidation-phase3-internalise-helpers** — 依赖 P1 `aigc-tool-consolidation-phase2-unregister-originals`。Phase 2 之后原 4 个 Tool 类不再注册但仍是 `Tool<I,O>` — Phase 3 把它们从 Tool 接口剥离, 改为 internal `AigcGenerator` 接口 + 4 个 sealed impl (Image / Video / Music / Speech)。dispatcher 直接持有 generator instances。**方向：** 重命名 `GenerateImageTool` → `AigcImageGenerator: AigcGenerator`，类似处理其他 3 个；删除 `Tool<I, O>` 实现；同 cycle 删除现有 4 个 tool 的独立 unit test 文件 (它们的逻辑被 dispatcher 测试覆盖)，或者重构它们到 generator-targeting tests。Rubric §5.6 / §3a-3。Milestone §later. · skipped 2026-05-02: scope assessed as multi-cycle big-bang — 4 prod-file rewrites (drop Tool<I,O> contract, drop id/inputSchema/permission/applicability overrides) + 5 integration tests (ReplayLockfileToolTest / LockfileCompletenessE2ETest / RefactorLoopE2ETest / CrossPathSourceSharedTest / ProviderFallbackE2ETest) all hand-register the 4 tools to ToolRegistry to invoke them by id, requiring migration to aigc_generate dispatcher's sealed Input shape + 4 unit-test files (~1500 LOC total). Partial-state risk identical to debt-split-session-action-tool-input precedent. Token win is ~0 (these tools are already unregistered post phase 2; LLM never sees them). Needs explicit phasing into sub-bullets (phase 3a: introduce AigcGenerator interface alongside Tool / phase 3b: migrate integration tests / phase 3c: delete Tool impls).
- **re-evaluate-cross-engine-incremental-render-android** — Android per-clip render cache。**方向：** 用户决定 promote / demote / delete. Rubric §5.3 / §5.7。Milestone §later. · skipped 2026-05-02: Android 平台窗口仍未开 (user 仅 unblock desktop).
- **re-evaluate-cross-engine-incremental-render-ios** — iOS per-clip render cache。**方向：** 同上 mirror Android. Rubric §5.3 / §5.7。Milestone §later. · skipped 2026-05-02: iOS 平台窗口仍未开.
- **re-evaluate-effect-kind-registry-cross-engine-tax** — 3-platform refactor without concrete next-effect-kind driver. **方向：** 用户决定 promote / demote / delete. Rubric §5.2 / §5.6。Milestone §later. · skipped 2026-04-25: meta bullet (driver-gated; 没有具体新 effect kind).
- **re-evaluate-desktop-merge-conflict-strategy** — `talevia merge` CLI + 3-way merge across timeline / source / lockfile. **方向：** 用户决定 promote / demote / delete. Rubric §5.4 / §3.4。Milestone §later. · skipped 2026-04-25: meta bullet (driver-gated; 没有真实多用户 conflict 报告).

## P2 — 记债 / 观望

- **debt-tool-count-snapshot-60-2026-05-02** — R.5 #1 + R.6 #1 scan refresh @ 2026-05-02: `core/tool/builtin/**/*Tool.kt` count = 60。**方向：** observational. **触发条件：** next repopulate count > 60 OR 单领域出现 ≥ 2 同前缀 prefix-pair → promote `debt-shrink-tool-spec-surface` 到 P0/P1。Rubric §5.6。Milestone §later.
- **debt-todo-snapshot-0-2026-05-02** — R.5 #6 anchored-regex scan @ 2026-05-02: 0 code-comment-form TODO/FIXME/HACK/XXX in `core/commonMain`. **方向：** observational. **触发条件：** next repopulate anchored-regex count > 0 → real `// TODO:` debt landed. Rubric §5.6。Milestone §later.
- **debt-long-file-snapshot-2026-05-02-v2** — R.5 #4 scan refresh @ 2026-05-02: SessionActionTool 606 (P0 split work imminent), SqlDelightSessionStore 449, ProviderQueryTool 403, BusEvent.kt 401 (was 374 cycle 18). **方向：** observational. **触发条件：** any top-10 file ≥ 700 LOC → forced P0 split. Rubric §5.6。Milestone §later.
- **debt-tool-spec-token-snapshot-14974-2026-05-02** — R.6 #1 audit @ 2026-05-02: 38 tools / 14,974 tokens / 59,871 bytes. P0 aigc consolidation 预期下降 ~700-1000 tokens (phase 2 落地后). **方向：** observational. **触发条件：** next repopulate audit token > 15,500 OR audit tool count > 40 — promote `debt-shrink-tool-spec-surface` to P0. Rubric §5.7。Milestone §later.
- **debt-project-field-snapshot-18-2026-05-02** — R.5 #3 scan @ 2026-05-02: `Project.kt` 顶层 `val` 字段数 = 18. **方向：** observational. **触发条件：** next repopulate field count > 18 AND 新增字段是 List<X> / Map<K, V> 类型. Rubric §5.6 / §3a-3。Milestone §later.
- **debt-unified-dispatcher-select-plugin-shape** — `project_query` 现已 22 selects；`rejectIncompatibleProjectQueryFilters` 已 24 规则；dispatcher 仍是单文件 monolith。**方向：** 当 select 数 ≥ 25 OR 规则 ≥ 30 时实施 select-plugin registry 或按主题拆 dispatcher。Rubric 外 / 顺手记录。Milestone §later.
- **debt-ios-filter-parity-test** — iOS AVFoundation filter dispatch 单 gate (manifest only)。**方向：** add iOS-side test introspecting `AVFoundationVideoEngine` filter dispatch。**触发条件：** iOS Swift filter dispatch grows / shrinks OR iOS 平台窗口打开 OR real-world iOS render reports missing effect. Rubric 外 / 顺手记录。Milestone §later.
- **agent-tool-streaming-text-delta** — Generic `BusEvent.ToolStreamingPart` for chunk-emitting tools. **方向：** ship BusEvent + Metrics + ServerDtos arms with first chunk-emitting tool. **触发条件：** ≥ 1 chunk-emitting tool lands. Rubric §5.4。Milestone §later.
- **m9-binding-conflict-detection-seed** — VISION §3.3 / M9-candidate. Multi-binding clips 当前 blind-concat-folded in `AigcPipeline.foldPrompt(...)`. **方向：** static `project_query(select=binding_conflicts)` scan over each clip's bound source nodes. **触发条件：** M9 promotion (§4 GUI mostly done) OR real user-report. Rubric §3.3 / §5.5。Milestone §later.
- **debt-split-bus-event** — `core/bus/BusEvent.kt` 401 LOC（cycle 18 23ebdc7f 加 `BusEvent.ProjectMutated` +27 LOC）。**方向：** flatten 23 nested sealed-type variants 到 top-level + rename ~40+ `is BusEvent.X` patterns。Rubric §5.6 / §3a-3。Milestone §later. · skipped 2026-05-02: trigger 条件未触发 — 401 < R.5 #4 500-LOC 阈值. Re-evaluate when BusEvent.kt ≥ 500 LOC.
- **debt-lockfile-lazy-interface-O1-open** — Phase 2a (cycle 28) shrunk talevia.json envelope (mutate cost O(1)). Phase 2b 仍未做：`Project.lockfile.entries` 仍在 openAt 时全量 materialise (O(N)); `findByInputHash` / `findByAssetId` 通过 in-memory `Map<,>` index 满足 O(1) lookup 但 N 大时启动开销线性增长。30+ callers do `entries.size` / `entries.filter` / `entries.fold` / `entries.last` 跨 `core/tool/builtin/project/**`, `core/domain/ProjectStaleness*`, `AigcBudgetGuard`, `ReplayLockfileTool`, `RegenerateStaleClipsTool` 等。**方向：** introduce `Lockfile` lazy interface（`stream(): Sequence<LockfileEntry>` + offset-indexed `findByInputHash` / `findByAssetId`）; 30+ callers 迁移到 stream / lookup-by-key (避免 forced-materialisation); add `FileProjectStoreOpenAtBenchmark` N=2000/5000 cases 验证 O(1) open。Rubric §5.7 / §5.3 / §3a-3。Milestone §later.
