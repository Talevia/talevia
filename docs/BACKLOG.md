# Backlog

`/iterate-gap` 的驱动源。每个 cycle 的流程：

1. 读这个文件，挑**最靠上的未完成**任务（P0 → P1 → P2，同档内按出现顺序）。
2. plan → 实现 → 验证 → 归档决策 → commit + push。
3. 在 iteration 的 commit 里顺手把本任务的 bullet 从这个文件里删掉（不是打钩，直接删），保持列表紧凑。
4. 如果这个文件的 P0/P1/P2 三档都已清空（或文件不存在），`/iterate-gap` 会按 skill 里的 rubric 分析一次性生成 **20 条新任务**写回这个文件、commit，然后**在同一个 cycle 里继续**挑新生成列表里第 1 条开干。

任务描述只给 **Gap（现状 / 痛点）** 和 **方向（期望动的东西）** —— 细节留给 cycle 里的 plan 阶段现读代码决定。Rubric 轴用 `§5.x` 标。

---

## P0 — 高杠杆、下一步就该动


## P1 — 中优，做完 P0 再排

- **re-evaluate-debt-aigc-tool-consolidation** — `debt-aigc-tool-consolidation` 跨 9+ repopulate cycles 持续 skip-tagged（自 cycle 113 起，原因 §3a-7：`LockfileEntry.toolId` 已 stamped `"generate_image"` 等在 on-disk bundle，删旧工具会让 `ReplayLockfileTool` 对老 entry 失效）。**方向：** 用户决定 promote (设计 alias-map: `"generate_image" → "aigc_generate" + kind="image"` 写进 ReplayLockfileTool) / demote (接受 4 个独立工具的 LLM context tax) / delete (criterion 不再相关)。Rubric §5.6 / §5.7 / §3a #1 / §3a #7。Milestone §later. · skipped 2026-04-25: meta bullet awaiting user decision (promote / demote / delete).
- **re-evaluate-debt-split-session-action-tool-input** — `debt-split-session-action-tool-input` 跨 ≥ 3 repopulate cycles skip-tagged for scope: 14 verbs × handler signature change × 13 test files × dispatcher refactor ≈ 600 LOC blast radius across 22+ files。**方向：** 用户决定 promote (commit to phased decomposition: Phase 1 sealed Input + dispatcher / Phase 2 handler signatures / Phase 3 LLM schema oneOf — split into 3 dedicated bullets), demote (accept the 606-LOC Input optional-soup until SessionActionTool grows past 800 / forced P0), or delete (live with the LOC; SessionAction is a leaf tool whose surface stabilises). Rubric §5.6 / §3a-3。Milestone §later. · skipped 2026-05-02: meta bullet (was P1; converted from `debt-split-session-action-tool-input` after ≥ 3 skip cycles).
- **re-evaluate-cross-engine-incremental-render-android** — `cross-engine-incremental-render-android` 跨 ≥ 7 repopulate cycles skip-tagged: CLAUDE.md "Platform priority" 把 Android 推迟到 desktop CLI parity 之后，且 iOS / Android 维持不退化, 不主动扩新特性。**方向：** 用户决定 promote (real Android export wall-time complaint or Android user driver materialises), demote (accept whole-timeline re-encode on Android indefinitely), or delete (Android per-clip render cache will land via different mechanism, e.g. Media3 Transformer 内部缓存改动)。Rubric §5.3 / §5.7。Milestone §later. · skipped 2026-05-02: meta bullet (was P1; converted from `cross-engine-incremental-render-android` after ≥ 7 cycles platform-priority blocked).
- **re-evaluate-cross-engine-incremental-render-ios** — Same Known-incomplete iOS variant; ≥ 7 cycles skip-tagged for the same platform-priority stance. **方向：** 用户决定 promote / demote / delete (mirror Android sibling decision). Rubric §5.3 / §5.7。Milestone §later. · skipped 2026-05-02: meta bullet (was P1; converted after ≥ 7 cycles platform-priority blocked).
- **re-evaluate-lockfile-incremental-load** — `lockfile-incremental-load` 跨 4+ repopulate cycles 持续 skip-tagged with the same scope reason: multi-file write atomicity (envelope ↔ lockfile.jsonl tx) needs explicit user signoff on bundle-inconsistency-on-crash trade-off; no real-world > 500-entry project to validate against `FileProjectStoreOpenAtBenchmark`. **方向：** 用户决定 promote (define the atomicity contract — fsync order or two-phase write or accept best-effort), demote (acknowledge O(N) lockfile re-decode is acceptable until N really hurts), or delete (lockfile is unlikely to grow large in practice). Rubric §5.7 / §5.3 / §3a-3。Milestone §later. · skipped 2026-04-25: meta bullet (was P0; converted to re-evaluate after 3+ skip cycles).
- **re-evaluate-aigc-result-multi-variant** — `aigc-result-multi-variant` 跨 4+ repopulate cycles persistently skip-tagged: §3a #4 (states should not be binary) + design ambiguity — bullet's "shared inputHash" semantic conflicts with seed-in-hash invariant (different seeds → different hashes), AND video providers (Sora 2 / Veo) don't natively support `n` so per-variant calls have separate-billing UX implications. **方向：** 用户决定 promote (specify the variant-key shape — variantIndex as a separate column not a hash override, per-variant billing UX baseline), demote (single-variant is fine until users ask), or delete. Rubric §5.2。Milestone §later. · skipped 2026-04-25: meta bullet (was P1; converted after 3+ skip cycles).
- **re-evaluate-effect-kind-registry-cross-engine-tax** — `effect-kind-registry-cross-engine-tax` 跨 4+ repopulate cycles skip-tagged: scope — 3-platform refactor without a concrete new effect kind to validate the registry against. **方向：** 用户决定 promote (next new effect kind addition pulls the registry refactor along as a prerequisite), demote (4 aligned kinds work fine via existing test coverage; defer indefinitely), or delete (assume cross-engine alignment will always be hand-coded). Rubric §5.2 / §5.6。Milestone §later. · skipped 2026-04-25: meta bullet (was P1; converted after 3+ skip cycles).
- **re-evaluate-desktop-merge-conflict-strategy** — `desktop-merge-conflict-strategy` 跨 4+ repopulate cycles skip-tagged: scope — new top-level `talevia merge` CLI command with semantic 3-way merge across timeline / source / lockfile is multi-cycle work; no real-world multi-user bundle conflict reported yet. **方向：** 用户决定 promote (commit to multi-cycle work; pick first slice — `.gitattributes` `merge=talevia-json` driver as MVP), demote (accept manual JSON merge until users complain), or delete. Rubric §5.4 / §3.4。Milestone §later. · skipped 2026-04-25: meta bullet (was P1; converted after 3+ skip cycles).

## P2 — 记债 / 观望

- **debt-tool-count-snapshot-60-2026-05-02** — R.5 #1 + R.6 #1 scan @ 2026-05-02: `core/tool/builtin/**/*Tool.kt` count = 60，与 cycle 153/154/162/17 baseline 一致。近似工具群无新 hit。**方向：** observational, **触发条件：** next repopulate count > 60 OR 单领域出现 ≥ 2 同前缀 prefix-pair → 立即 promote `debt-shrink-tool-spec-surface` 到 P0/P1。Rubric §5.6。Milestone §later.
- **debt-todo-snapshot-0-2026-05-02** — R.5 #6 anchored-regex scan refresh @ 2026-05-02: 0 code-comment-form TODO/FIXME/HACK/XXX in `core/commonMain` (cycle 16 4228de46 tightened R.5 #6 grep to `^\s*(//|/\*+|\*)` form). **方向：** observational; trigger watches *increases*. **触发条件：** next repopulate anchored-regex count > 0 → real `// TODO:` debt landed, generate `debt-clean-todos` cycle. Rubric §5.6。Milestone §later.
- **debt-long-file-snapshot-2026-05-02** — R.5 #4 scan refresh @ 2026-05-02: SessionActionTool 606 (flat), **ProjectStaleness.kt 528 (NEW; was 370, +158 from cycle 17 incrementalPlan + renderStaleClips additions)**, SqlDelightSessionStore 449 (flat), ProviderQueryTool 403 (flat), SourceNodeActionTool 396 (flat). 2 over §3a-3 500-LOC threshold (SessionActionTool 606, ProjectStaleness 528); both under R.5 #4 强制 P0 800-line. ProjectStaleness split work tracked in P0 `debt-split-project-staleness`. SessionActionTool split tracked in P1 `re-evaluate-debt-split-session-action-tool-input` (meta). **方向：** observational. **触发条件：** any top-10 file ≥ 700 LOC → forced P0 split bullet. Rubric §5.6。Milestone §later.
- **debt-tool-spec-token-snapshot-14974-2026-05-02** — R.6 #1 audit refresh @ 2026-05-02: `ToolSpecBudgetAuditTest` audit-subset = 38 tools / **14,974 tokens** / 59,871 bytes (was 16,585 / 38 / 66,313 @ 2026-05-01; -1,611 tokens / -9.7% via cycle 11 ff8d6388 schema trim + cycle 17 144071d1 helpText trim). Now **under** the M6 #1 strict 15k SOFT gate with 26 tokens of headroom. Top-by-tokens: project_query=998t (was 1,006), clip_action=996t, project_action=883t, session_action=880t, session_query=875t. **方向：** observational. **触发条件：** next repopulate audit token > 15,500 OR audit tool count > 40 — promote `debt-shrink-tool-spec-surface` to P0 if either fires. Rubric §5.7。Milestone §later.
- **debt-project-field-snapshot-18-2026-05-02** — R.5 #3 scan refresh @ 2026-05-02: `Project.kt` 顶层 `val` 字段数 = 18 (flat from cycle 14 baseline). 已知 List/Map 字段（append-only 语义）：`assets`、`source`（DAG）、`snapshots`、`lockfile.entries`、`renderCache.entries`、`clipRenderCache.entries`. **方向：** observational. **触发条件：** next repopulate field count > 18 AND 新增字段是 List<X> / Map<K, V> 类型 → promote SQLDelight-table extraction bullet. Rubric §5.6 / §3a-3。Milestone §later.
- **debt-unified-dispatcher-select-plugin-shape** — `project_query` 现已 22 selects (cycle 17 加 `render_stale` + `incremental_plan` 跨过 §3a #12 trigger 阈值 ≥ 20)；`rejectIncompatibleProjectQueryFilters` 已 24 规则；dispatcher 仍是单文件 monolith，每加 1 select 改动散布 5 处 (constants / ALL_SELECTS / Input field / rowSerializerFor arm / execute arm) + 1 helpText line + 0..N schema properties + 0..N filter-guard 规则。**方向：** 当 select 数 ≥ 25 OR `rejectIncompatibleProjectQueryFilters` 规则 ≥ 30 时实施 select-plugin registry — `interface QuerySelect<R>` 一等对象（id / row serializer / filter compatibility / runner / helpText line），dispatcher 退化为 `selects.find { it.id == select }?.run(project, input, …)`；或按主题拆 dispatcher (`project_query` 读投影 / `project_diff_query` diff 系列 / `project_plan_query` incremental plan + render_stale + 未来 plan)。Rubric 外 / 顺手记录。Milestone §later.
- **debt-ios-filter-parity-test** — Cycle 9 introduced `FilterKind` enum + Cycle 10's `FilterCrossEngineParityTest` covers FFmpeg / Media3 / iOS via the Kotlin `FilterEngineCapabilities` manifest. Kotlin engines are double-gated (sealed `when` compile error + manifest test). iOS AVFoundation is **single-gated** — only the manifest catches drift, and the manifest is a manual mirror of `AVFoundationVideoEngine.swift:71`'s `switch` cases. If iOS Swift drops `case "vignette"` silently, the Kotlin test passes (manifest still claims support), and iOS clips render with no vignette at runtime. **方向：** add an iOS-side test (xcodebuild path or Swift unit test) that introspects `AVFoundationVideoEngine` filter dispatch and asserts coverage matches `FilterEngineCapabilities.avFoundationIos`. Could be a Swift-side enumeration mirroring Kotlin's `FilterKind.entries` (via SKIE-bridged sealed type). **触发条件：** iOS Swift filter dispatch grows / shrinks OR a real-world iOS render reports missing effect. Rubric 外 / 顺手记录。Milestone §later.
- **desktop-agent-step-notice** — CLI now renders `Step N · processing…` on every `BusEvent.AgentRunStateChanged`-into-Generating edge (cycle 166's `small-user-progress-surface` shipped CLI half only). Desktop UI doesn't yet subscribe to `BusEvent.AgentRunStateChanged` at all — multi-step trajectories still go silent for 5–30 s between tool dispatches in the Compose Desktop chat panel. M7 #3 同 intent；M7 promotion logic 把它列为"opportunistic per CLAUDE.md Platform priority"。**方向：** subscribe in the desktop chat composable, render an analogous step-progress chip / inline line. **触发条件：** Desktop chat panel UX work resumes / user reports the silence. Rubric §5.4 / desktop。Milestone §M7.
- **agent-tool-streaming-text-delta** — When Talevia eventually adds a tool that emits textual / byte-stream deltas (shell exec, log-tail, chunked file read, web search streaming), it'll need a generic `BusEvent.ToolStreamingPart(callId, partId?, deltaText?, sizeBytes?, message?)`. Today no such tool exists: AIGC uses lifecycle-tick `BusEvent.AigcJobProgress`, FFmpeg/Export uses per-frame `Part.RenderProgress`, ReadFileTool is synchronous. **方向：** ship the BusEvent + EventBusMetricsSink + ServerDtos exhaustive `when` arm together with the first chunk-emitting tool. **触发条件：** ≥ 1 chunk-emitting tool lands. Rubric §5.4。Milestone §later.
