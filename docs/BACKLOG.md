# Backlog

`/iterate-gap` 的驱动源。每个 cycle 的流程：

1. 读这个文件，挑**最靠上的未完成**任务（P0 → P1 → P2，同档内按出现顺序）。
2. plan → 实现 → 验证 → 归档决策 → commit + push。
3. 在 iteration 的 commit 里顺手把本任务的 bullet 从这个文件里删掉（不是打钩，直接删），保持列表紧凑。
4. 如果这个文件的 P0/P1/P2 三档都已清空（或文件不存在），`/iterate-gap` 会按 skill 里的 rubric 分析一次性生成 **20 条新任务**写回这个文件、commit，然后**在同一个 cycle 里继续**挑新生成列表里第 1 条开干。

任务描述只给 **Gap（现状 / 痛点）** 和 **方向（期望动的东西）** —— 细节留给 cycle 里的 plan 阶段现读代码决定。Rubric 轴用 `§5.x` 标。

---

## P0 — 高杠杆、下一步就该动

- **debt-split-session-action-tool-input** — `SessionActionTool.kt` 606 LOC, `Input` 17 个 optional 字段。User authorization 2026-05-02 解除 big-bang 风险卡点 — 不要兼容性。**方向：** sealed `Input` + 每 verb 一个 data class（14 个：`ArchiveInput / RenameInput / DeleteInput / ForkInput / RevertInput / CompactInput / ListInput` 等）；用 kotlinx.serialization `@JsonClassDiscriminator("action")` 让 LLM JSON Schema 用 oneOf；一次性 big-bang refactor — handler signatures 改 + 13 test files 同 cycle 内更新；破坏 `Part.Tool.input` 已持久化 JSON (旧 session 无法 decode — acceptable)。Rubric §5.6 / §3a-3。Milestone §later. · skipped 2026-05-02: scope assessed mid-dispatch as ~2000 LOC mechanical change across 30+ files. 单 cycle big-bang 风险高 (mid-refactor partial-state commit / 测试串扰). Needs explicit phasing into sub-bullets (phase 1: sealed Input + dispatcher only / phase 2: handler signatures / phase 3: test migration).
- **debt-shrink-tool-spec-surface** — R.6 #1 trigger fired: `*Tool.kt` count = 61 ≥ 40 threshold. LLM-facing tool spec per-turn cost is the dominant context-tax driver. Several near-duplicate prefix groups (`project_action` / `project_maintenance_action` / `project_pin_action` / `project_query` / `project_snapshot_action` — 5 同前缀；similar for `clip_action` / `clip_set_action` / `filter_action` / `track_action` / `transition_action`). **方向：** consolidate per-area dispatchers (collapse same-prefix tools into a single `kind=`-discriminated dispatcher mirroring `aigc_generate`'s phase 2 win); trim helpText / schema descriptions on the heaviest dispatchers; target audit-subset reduction ≥ 1k tokens. Rubric §5.7 / §5.6。Milestone §later. · skipped 2026-05-02: helpText/schema-trim arm exhausted (cycle 5 42b71191 trimmed project_query helpText; cycle 11 ff8d6388 trimmed top-5 dispatcher descriptions for -1,449 tokens to 14,982). Audit-subset currently 14,974 < SOFT 15,000 cap — passing the strict gate with 26 tokens headroom. Top-5 dispatchers (project_query 998t, clip_action 996t, project_action 883t, session_action 880t, session_query 875t) all have already-trimmed helpText + schema descriptions; further marginal trim risks losing load-bearing semantic guidance. Real ≥1k savings only achievable via structural per-area dispatcher consolidation (5 project_*_action → 1 dispatcher, 5 *_action → 1 timeline_action, etc.) — multi-cycle work needing explicit phasing into per-area sub-bullets at next repopulate.

## P1 — 中优，做完 P0 再排

- **aigc-tool-streaming-first-emitter** — Cycle 35 5653cb99 shipped `BusEvent.ToolStreamingPart` event surface + metrics + SSE wiring. Bullet text deferred "first emitter 留下次" — this is that bullet. Pick a tool whose execution is naturally chunkable and wire it: candidate is `synthesize_speech` (TTS providers expose streaming response chunks), or a new `draft_script` text-generation tool (LLM-routed text output). Validates the cycle-35 wiring against a real producer + first UI consumer (CLI Renderer / desktop ChatPanel). **方向：** pick **one** chunkable tool, plumb `ctx.publishEvent(BusEvent.ToolStreamingPart(...))` calls during the streaming provider response, integration test that asserts ≥ 2 chunks land before the final ToolResult. Rubric §5.4 / §5.2。Milestone §later. · skipped 2026-05-02: every plausible first-emitter requires multi-cycle infrastructure additions before "≥ 2 chunks during execution" can be meaningfully wired. (a) `synthesize_speech` — TtsEngine.synthesize() returns a single `SynthesizedAudio` blob (no streaming overload); to stream chunks during the provider response, need new `synthesizeStreaming(request, onChunk: (ByteArray) -> Unit)` overload + OpenAi/test-fake impls — cross-cutting engine-API change. (b) `draft_script` — new genre-specific tool (script writing) violates §3a #5 (Core 不硬编码 genre 概念). (c) BashTool — `ProcessRunner.run()` returns final stdout atomically; needs new `runStreaming(...)` API + Jvm/Native impls — multiplatform infra change. (d) Synthetic post-hoc chunking (split final result into N events) — doesn't actually stream during execution, defeats the bullet's "during the streaming provider response" requirement. Real first-emitter needs explicit phasing into engine-API extension sub-bullets first.
- **debt-lockfile-lazy-interface-O1-open** — Phase 2a (cycle 28 59746ec5) shrunk talevia.json envelope (mutate cost O(1)); phase 2b 仍未做：`Project.lockfile.entries` 仍在 openAt 时全量 materialise (O(N)); 30+ callers do `entries.size` / `entries.filter` / `entries.fold` / `entries.last` 跨 `core/tool/builtin/project/**`, `core/domain/ProjectStaleness*`, `AigcBudgetGuard`, `ReplayLockfileTool`, `RegenerateStaleClipsTool` 等。**方向：** introduce `Lockfile` lazy interface（`stream(): Sequence<LockfileEntry>` + offset-indexed `findByInputHash` / `findByAssetId`）; 30+ callers 迁移到 stream / lookup-by-key (避免 forced-materialisation); add `FileProjectStoreOpenAtBenchmark` N=2000/5000 cases 验证 O(1) open。Rubric §5.7 / §5.3 / §3a-3。Milestone §later. · skipped 2026-05-02: 30+ caller migration is multi-cycle big-bang (deferred 4 cycles: 28 / 30 / 32 / 35). Needs explicit phasing into sub-bullets — phase 2b-1: introduce `Lockfile` as interface alongside the data class with pass-through impl; phase 2b-2: migrate 30+ callers to stream / key-lookup APIs (mechanical mass-edit); phase 2b-3: replace EagerLockfile with LazyLockfile reading JSONL on-demand (the actual O(1)-open perf win). Each sub-phase is independently shippable.
- **re-evaluate-cross-engine-incremental-render-android** — Android per-clip render cache。**方向：** 用户决定 promote / demote / delete. Rubric §5.3 / §5.7。Milestone §later. · skipped 2026-05-02: Android 平台窗口仍未开 (user 仅 unblock desktop).
- **re-evaluate-cross-engine-incremental-render-ios** — iOS per-clip render cache。**方向：** 同上 mirror Android. Rubric §5.3 / §5.7。Milestone §later. · skipped 2026-05-02: iOS 平台窗口仍未开.
- **re-evaluate-effect-kind-registry-cross-engine-tax** — 3-platform refactor without concrete next-effect-kind driver. **方向：** 用户决定 promote / demote / delete. Rubric §5.2 / §5.6。Milestone §later. · skipped 2026-04-25: meta bullet (driver-gated; 没有具体新 effect kind).
- **re-evaluate-desktop-merge-conflict-strategy** — `talevia merge` CLI + 3-way merge across timeline / source / lockfile. **方向：** 用户决定 promote / demote / delete. Rubric §5.4 / §3.4。Milestone §later. · skipped 2026-04-25: meta bullet (driver-gated; 没有真实多用户 conflict 报告).

## P2 — 记债 / 观望

- **debt-tool-count-snapshot-61-2026-05-02-v2** — R.5 #1 + R.6 #1 scan refresh @ 2026-05-02 (cycle 36): `core/tool/builtin/**/*Tool.kt` count = 61 (unchanged from cycle 31 baseline; phase-3a's `AigcGenerator.kt` wasn't a `*Tool.kt` file). **方向：** observational. **触发条件：** next repopulate count > 61 OR 单领域出现 ≥ 3 同前缀 prefix-group → promote `debt-shrink-tool-spec-surface` 到 P0/P1。Rubric §5.6。Milestone §later.
- **debt-todo-snapshot-0-2026-05-02-v2** — R.5 #6 anchored-regex scan @ 2026-05-02 (cycle 36): 0 code-comment-form TODO/FIXME/HACK/XXX in `core/commonMain` (unchanged from cycle 31). **方向：** observational. **触发条件：** next repopulate anchored-regex count > 0 → real `// TODO:` debt landed. Rubric §5.6。Milestone §later.
- **debt-long-file-snapshot-2026-05-02-v4** — R.5 #4 scan refresh @ 2026-05-02 (cycle 36): SessionActionTool 606 (P0 split work pending), GenerateImageTool 568 (NEW since cycle 31; cycle 33 native-batch +208 LOC; P1 split bullet active above), SqlDelightSessionStore 449, BusEvent.kt 449 (was 401 cycle 31; +48 LOC from cycle 35's ToolStreamingPart), FileProjectStore 405, ProviderQueryTool 403, SourceNodeActionTool 387, Agent 384, SessionQueryTool 378。**方向：** observational. **触发条件：** any top-10 file ≥ 800 LOC → forced P0 split. Rubric §5.6。Milestone §later.
- **debt-tool-spec-token-snapshot-14982-2026-05-02-v2** — R.6 #1 audit @ 2026-05-02 (cycle 36): 38 tools / 14,982 tokens in audit-subset (last measurement; cycle 11 ff8d6388 trim landed at SOFT 15k strict gate; unchanged). **方向：** observational. **触发条件：** next repopulate audit token > 15,500 OR audit tool count > 40 (excluding AIGC) → promote `debt-shrink-tool-spec-surface` to P0. Rubric §5.7。Milestone §later.
- **debt-project-field-snapshot-18-2026-05-02-v2** — R.5 #3 scan @ 2026-05-02 (cycle 36): `Project.kt` 顶层 `val` 字段数 = 18 (unchanged from cycle 31). **方向：** observational. **触发条件：** next repopulate field count > 18 AND 新增字段是 List<X> / Map<K, V> 类型. Rubric §5.6 / §3a-3。Milestone §later.
- **debt-unified-dispatcher-select-plugin-shape** — `project_query` 现已 22 selects；`rejectIncompatibleProjectQueryFilters` 已 24 规则；dispatcher 仍是单文件 monolith。**方向：** 当 select 数 ≥ 25 OR 规则 ≥ 30 时实施 select-plugin registry 或按主题拆 dispatcher。Rubric 外 / 顺手记录。Milestone §later.
- **debt-ios-filter-parity-test** — iOS AVFoundation filter dispatch 单 gate (manifest only)。**方向：** add iOS-side test introspecting `AVFoundationVideoEngine` filter dispatch。**触发条件：** iOS Swift filter dispatch grows / shrinks OR iOS 平台窗口打开 OR real-world iOS render reports missing effect. Rubric 外 / 顺手记录。Milestone §later.
- **m9-binding-conflict-detection-seed** — VISION §3.3 / M9-candidate. Multi-binding clips 当前 blind-concat-folded in `AigcPipeline.foldPrompt(...)`. **方向：** static `project_query(select=binding_conflicts)` scan over each clip's bound source nodes. **触发条件：** M9 promotion (§4 GUI mostly done) OR real user-report. Rubric §3.3 / §5.5。Milestone §later.
- **debt-split-bus-event** — `core/bus/BusEvent.kt` 449 LOC（cycle 35 5653cb99 加 `BusEvent.ToolStreamingPart` +48 LOC; was 401 cycle 31）。**方向：** flatten 23 nested sealed-type variants 到 top-level + rename ~40+ `is BusEvent.X` patterns。Rubric §5.6 / §3a-3。Milestone §later. · skipped 2026-05-02: trigger 条件未触发 — 449 < R.5 #4 500-LOC 阈值 (closer than cycle 31's 401, but still under). Re-evaluate when BusEvent.kt ≥ 500 LOC.
