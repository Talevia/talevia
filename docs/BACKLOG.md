# Backlog

`/iterate-gap` 的驱动源。每个 cycle 的流程：

1. 读这个文件，挑**最靠上的未完成**任务（P0 → P1 → P2，同档内按出现顺序）。
2. plan → 实现 → 验证 → 归档决策 → commit + push。
3. 在 iteration 的 commit 里顺手把本任务的 bullet 从这个文件里删掉（不是打钩，直接删），保持列表紧凑。
4. 如果这个文件的 P0/P1/P2 三档都已清空（或文件不存在），`/iterate-gap` 会按 skill 里的 rubric 分析一次性生成 **20 条新任务**写回这个文件、commit，然后**在同一个 cycle 里继续**挑新生成列表里第 1 条开干。

任务描述只给 **Gap（现状 / 痛点）** 和 **方向（期望动的东西）** —— 细节留给 cycle 里的 plan 阶段现读代码决定。Rubric 轴用 `§5.x` 标。

---

## P0 — 高杠杆、下一步就该动

- **debt-split-timeline-action-tool** — Cycle 49's transition absorption pushed TimelineActionTool.kt to 579 LOC (was 285 as TrackActionTool); now over R.5 #4's 500-LOC default-P1 threshold. The track + transition handler bodies (`executeAdd / executeRemove / executeDuplicate / executeReorder` for tracks; `executeTransitionAdd / executeTransitionRemove` inlined this cycle) all sit in the dispatcher class. Mirrors cycle 152's ClipActionTool 800-LOC split which extracted handlers into sibling files (TrackActionHandlers.kt already exists for the 4 track verbs — phase 1a kept that split intact). **方向：** extract the 2 transition handlers + their helpers (`pickEffectTrack` / `trackKindOf` / `TRANSITION_ASSET_PREFIX`) into new `TransitionActionHandlers.kt` sibling; TimelineActionTool drops back to ~400 LOC. Pure refactor; no behaviour / schema / wire-format change. Forward-compat for phase 1b-impl (filter absorption — needs space). Rubric §5.6 / §3a-3。Milestone §later.
- **debt-split-session-action-tool-input-phase1b** — Phase 1b sub-bullet of `debt-split-session-action-tool-input` (cycle 47 phasing breakdown). Phase 1a (cycle 47 3eebe6a8) introduced `SessionVerb` sealed-interface family + `Input.toVerb()` decoder as pure addition. Phase 1b rewrites `SessionActionTool.execute`'s `when (input.action)` to consume `input.toVerb()` + typed `is SessionVerb.X ->` arms. Single-cycle scope, ~30 LOC delta in SessionActionTool.kt only — no handler / test changes (phase 1c covers handlers, phase 1d covers tests). **方向：** wrap the 14-arm dispatcher in `when (val verb = input.toVerb())`; each arm uses smart-cast typed access (`is SessionVerb.Archive -> handler(...)`); no signature changes downstream. Rubric §5.6 / §3a-3。Milestone §later.
- **debt-lockfile-lazy-interface-phase2b1b** — Phase 2b-1b sub-bullet of `debt-lockfile-lazy-interface-O1-open` (cycle 47 re-phasing). Phase 2b-1a (cycle 48 d0b0eb21) added `Lockfile.stream(): Sequence<LockfileEntry>` as pure addition; `findByInputHash` + `findByAssetId` already existed (cycle-28 era). Phase 2b-1b migrates 30+ caller sites from `entries.firstOrNull { ... }` / `entries.size` / `entries.fold` / `entries.last` patterns to `stream() / findByInputHash / findByAssetId` — mechanical mass-edit across `core/tool/builtin/project/**`, `core/domain/ProjectStaleness*`, `AigcBudgetGuard`, `ReplayLockfileTool`, `RegenerateStaleClipsTool`. **方向：** mechanical sed across ~30 files; replace `entries.firstOrNull { it.inputHash == h }` → `findByInputHash(h)`, `entries.firstOrNull { it.assetId == a }` → `findByAssetId(a)`, `entries.asSequence()` → `stream()`. Phase 2b-1c (post-1b) does the eager → lazy data-class-to-interface lift. Rubric §5.7 / §5.3。Milestone §later.
- **debt-tool-consolidation-project-action-phase1-design** — Cycle 50 skip-tag identified design ambiguity in cycle 47's `debt-tool-consolidation-project-action-phase1` bullet: existing `project_action` is ALREADY a 6-verb dispatcher, so absorbing 3 sibling project_*_action tools into it requires either renaming the existing tool OR using a different id (which expands surface). **方向：** decide the id strategy at this design-decision sub-bullet level — recommended option (a) rename existing `project_action` → `project_lifecycle_action` to free the namespace, then introduce new `project_action(kind=lifecycle|maintenance|pin|snapshot)` dispatcher; option (b) use `project_command(kind=...)` as the unifying name. Once decided, draft phase 1a-impl bullet to land the dispatcher per the chosen pattern. Single-cycle decision: 1 design note added to BACKLOG explaining the choice + rationale. Rubric §5.7 / §3a-1。Milestone §later.

## P1 — 中优，做完 P0 再排

- **debt-tool-consolidation-timeline-action-phase1b-design** — Cycle 50 skip-tag identified design ambiguity in cycle 47's `debt-tool-consolidation-timeline-action-phase1b` bullet: filter absorption needs (a) typealias retype that fattens TimelineActionTool.Input past R.5 #4 800-LOC threshold, OR (b) nested-Input arm (`TimelineActionTool.FilterArgs`) breaking cycle 44/49 flat-shape precedent. **方向：** decide the pattern at this design-decision sub-bullet — recommended (b) nested-Input given file-size pressure post phase 1a; document the precedent break in commit body + ENGINEERING_NOTES so future absorption cycles know the convention now allows nested-Input for sufficiently-distinct sub-domains. Once decided, draft phase 1b-impl bullet. Single-cycle decision: 1 design note + convention update. Rubric §5.7 / §3a-1。Milestone §later.
- **debt-shrink-tool-spec-surface** — Meta-gate for the per-area consolidation phases. R.6 #1 audit @ 2026-05-02: 14,982 tokens / 38 audit-subset tools (cycle 36 baseline). Cycle 44 absorbed `clip_set_action` into `clip_action` (~1k tokens saved); cycle 49 absorbed `transition_action` into `timeline_action` (~700 tokens saved). After phase 1b lands (filter absorption) + project consolidation lands, audit re-measurement should show ≥ 1.5k tokens cumulative reduction. **触发条件：** all three per-area phase-1 bullets done → re-measure tool_spec_budget; if reduction < 1.5k tokens cumulative, draft phase-2 bullets (tool body merging instead of just dispatcher consolidation). Rubric §5.7。Milestone §later.
- **debt-split-clip-action-tool-input-seed** — Cycle 44's clip_action consolidation grew the file from 291 → 377 LOC (+86 LOC for 3 new verbs + 6 new data classes + 3 new result types). Now 12 verbs, ~16 nullable Input fields, ~13 Output result lists. Pattern matches the SessionActionTool 606-LOC trajectory (14 verbs, 17 nullable fields). **方向：** observational. **触发条件：** ClipActionTool.kt ≥ 500 LOC OR verb count > 14 → split via the same sealed-Input phasing as session_action. Rubric §5.6 / §3a-3。Milestone §later.
- **debt-unified-dispatcher-select-plugin-shape** — `project_query` 现已 22 selects；`rejectIncompatibleProjectQueryFilters` 已 24 规则；dispatcher 仍是单文件 monolith。**方向：** 当 select 数 ≥ 25 OR 规则 ≥ 30 时实施 select-plugin registry 或按主题拆 dispatcher。Rubric 外 / 顺手记录。Milestone §later.
- **m9-binding-conflict-detection-seed** — VISION §3.3 / M9-candidate. Multi-binding clips 当前 blind-concat-folded in `AigcPipeline.foldPrompt(...)`. **方向：** static `project_query(select=binding_conflicts)` scan over each clip's bound source nodes. **触发条件：** M9 promotion (§4 GUI mostly done) OR real user-report. Rubric §3.3 / §5.5。Milestone §later.

## P2 — 记债 / 观望

- **debt-tool-count-snapshot-55-2026-05-02-v5** — R.5 #1 + R.6 #1 scan refresh @ 2026-05-02 (cycle 52): `core/tool/builtin/**/*Tool.kt` count = 55 (was 56 cycle 47 baseline; **net -1** because cycle 49 92650914 absorbed `TransitionActionTool` into `timeline_action(action=add_transition|remove_transition)`). **方向：** observational. **触发条件：** next repopulate count > 55 OR 单领域出现 ≥ 3 同前缀 prefix-group → promote `debt-shrink-tool-spec-surface` 到 P0/P1. Rubric §5.6。Milestone §later.
- **debt-todo-snapshot-0-2026-05-02-v5** — R.5 #6 anchored-regex scan @ 2026-05-02 (cycle 52): 0 code-comment-form TODO/FIXME/HACK/XXX in `core/commonMain` (unchanged since cycle 31). **方向：** observational. **触发条件：** next repopulate anchored-regex count > 0 → real `// TODO:` debt landed. Rubric §5.6。Milestone §later.
- **debt-long-file-snapshot-2026-05-02-v7** — R.5 #4 scan refresh @ 2026-05-02 (cycle 52): SessionActionTool 606 (P0 phase 1b dispatcher rewrite active above), **TimelineActionTool 579 (NEW since cycle 47; +294 LOC from cycle 49 transition absorption — see `debt-split-timeline-action-tool` P0 above)**, SqlDelightSessionStore 449, BusEvent.kt 449, FileProjectStore 405, ProviderQueryTool 403, SourceNodeActionTool 387, Agent 384, SessionQueryTool 378, ClipActionTool 377, ImportMediaTool 358. None ≥ 800 LOC. **方向：** observational. **触发条件：** any top-10 file ≥ 800 LOC → forced P0 split. Rubric §5.6。Milestone §later.
- **debt-tool-spec-token-snapshot-2026-05-02-v5** — R.6 #1 audit @ 2026-05-02 (cycle 52): last measured 14,982 tokens / 38 audit-subset tools (cycle 36 baseline). Cycle 50 e99f2c7d added a runtime gauge (`agent.tool_spec_budget.tokens` exposed via `GET /metrics`) — next repopulate can read live values rather than re-running the audit test. Cycles 44 + 49 consolidations should have dropped this by ~1.7k tokens cumulative. **方向：** observational. **触发条件：** runtime gauge > 15,500 OR audit tool count > 40 (excluding AIGC) → promote `debt-shrink-tool-spec-surface` to P0. Rubric §5.7。Milestone §later.
- **debt-project-field-snapshot-18-2026-05-02-v5** — R.5 #3 scan @ 2026-05-02 (cycle 52): `Project.kt` 顶层 `val` 字段数 = 18 (unchanged across cycles 31, 36, 42, 47, 52). **方向：** observational. **触发条件：** next repopulate field count > 18 AND 新增字段是 List<X> / Map<K, V> 类型. Rubric §5.6 / §3a-3。Milestone §later.
- **debt-ios-filter-parity-test** — iOS AVFoundation filter dispatch 单 gate (manifest only)。**方向：** add iOS-side test introspecting `AVFoundationVideoEngine` filter dispatch。**触发条件：** iOS Swift filter dispatch grows / shrinks OR iOS 平台窗口打开 OR real-world iOS render reports missing effect. Rubric 外 / 顺手记录。Milestone §later.
- **debt-split-bus-event** — `core/bus/BusEvent.kt` 449 LOC（cycle 35 5653cb99 加 `BusEvent.ToolStreamingPart` +48 LOC）。**方向：** flatten 23 nested sealed-type variants 到 top-level + rename ~40+ `is BusEvent.X` patterns。Rubric §5.6 / §3a-3。Milestone §later. · skipped 2026-05-02: trigger 条件未触发 — 449 < R.5 #4 500-LOC 阈值. Re-evaluate when BusEvent.kt ≥ 500 LOC.
- **re-evaluate-cross-engine-incremental-render** — Android + iOS per-clip render cache parity (FFmpeg JVM has it; M3 ExportTool dispatches `engine.supportsPerClipCache`)。**方向：** 用户决定 promote / demote / delete. Rubric §5.3 / §5.7。Milestone §later. · skipped 2026-05-02: 平台窗口仍未开 (user 仅 unblock desktop; Android/iOS 维持不退化底线).
- **re-evaluate-effect-kind-registry-cross-engine-tax** — 3-platform refactor without concrete next-effect-kind driver. **方向：** 用户决定 promote / demote / delete. Rubric §5.2 / §5.6。Milestone §later. · skipped 2026-04-25: meta bullet (driver-gated; 没有具体新 effect kind).
- **re-evaluate-desktop-merge-conflict-strategy** — `talevia merge` CLI + 3-way merge across timeline / source / lockfile. **方向：** 用户决定 promote / demote / delete. Rubric §5.4 / §3.4。Milestone §M8 (driver-gated). · skipped 2026-04-25: meta bullet (driver-gated; 没有真实多用户 conflict 报告).
